@{
    ViewData["Title"] = "SurveyApp — Dashboard";
}

<h2>Dashboard</h2>
<p>This proves the login → tenant-scoped API call chain works end-to-end.</p>

<h3>GET /me (SurveyApp.Api)</h3>
<pre>@ViewBag.MeResponse</pre>
